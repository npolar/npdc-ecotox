'use strict';
require('should');

describe('EcotoxicalTemplateEditController', function () {
  var $scope, $controller, $routeParams, EcologicalTemplate;

  // Set up mocks
  before(function () {
    $scope = {};
    $routeParams = {};
    EcotoxicalTemplate = {};
    $controller = function () {};
  });

  describe('#initFormula', function () {
    // no test...
  });
});
